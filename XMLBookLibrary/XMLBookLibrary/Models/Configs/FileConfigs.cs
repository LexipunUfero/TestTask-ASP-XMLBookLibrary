namespace XMLBookLibrary.Configs;

internal class FileConfigs
{
    internal string indexFile { get; } = "index";
    internal string authorDirectory { get; } = "authors";
    internal string bookDirectory { get; } = "books";
}